/* istanbul ignore file */
// This file is ignored in code coverage due to it not containing
// any meaningful functions.
console.log('zrc is still a work-in-progress, so this is a stub file');
process.exit(1);
