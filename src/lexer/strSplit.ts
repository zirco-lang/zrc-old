/**
 * Splits an input string into an array of single characters.
 * @param input The input string
 * @returns The input string, split into characters
 */
export default function strSplit(input: string): string[] {
    return input.split('');
}
